# SYSTEM_ANALYSIS_REPORT.md

## 1. API 엔드포인트 목록 및 기능 요약

### 1.1. UI 렌더링 API (Page Navigation)

`HomeController`는 서버 사이드 렌더링을 통해 HTML 페이지를 반환합니다.

| Method | Path                        | 기능                                   |
| ------ | --------------------------- | -------------------------------------- |
| GET    | `/`                         | 메인 페이지 (index.html) 반환        |
| GET    | `/reservations`             | 예약 목록 페이지 반환 (날짜 필터링 가능) |
| GET    | `/reservations/new`         | 새 예약 생성 폼 페이지 반환            |
| GET    | `/reservations/search`      | 예약 검색 페이지 반환                  |
| GET    | `/sites`                    | 캠핑장 목록 페이지 반환                |
| GET    | `/sites/{siteNumber}`       | 특정 캠핑장 상세 페이지 반환           |

### 1.2. RESTful API (`/api`)

`ReservationController`와 `SiteController`는 JSON 형식의 데이터를 반환하는 RESTful API를 제공합니다.

#### 예약 API (`/api/reservations`)

| Method   | Path             | 기능                                                       |
| -------- | ---------------- | ---------------------------------------------------------- |
| POST     | `/`              | 새 예약을 생성합니다.                                      |
| GET      | `/{id}`          | 특정 ID의 예약 정보를 조회합니다.                          |
| GET      | `/`              | 전체 예약 목록을 조회합니다. (날짜, 고객명으로 필터링 가능) |
| DELETE   | `/{id}`          | 특정 ID의 예약을 취소합니다. (확인 코드 필요)              |
| PUT      | `/{id}`          | 특정 ID의 예약 정보를 수정합니다. (확인 코드 필요)         |
| GET      | `/my`            | 고객 이름과 전화번호로 자신의 예약 목록을 조회합니다.      |
| GET      | `/calendar`      | 특정 캠핑장의 월별 예약 현황을 조회합니다.                 |

#### 캠핑장 API (`/api/sites`)

| Method | Path                        | 기능                                                         |
| ------ | --------------------------- | ------------------------------------------------------------ |
| GET    | `/`                         | 전체 캠핑장 목록을 조회합니다.                               |
| GET    | `/{siteId}`                 | 특정 ID의 캠핑장 상세 정보를 조회합니다.                     |
| GET    | `/{siteNumber}/availability`| 특정 날짜에 해당 캠핑장의 예약 가능 여부를 확인합니다.       |
| GET    | `/available`                | 특정 날짜에 예약 가능한 모든 캠핑장 목록을 조회합니다.       |
| GET    | `/search`                   | 주어진 기간에 예약 가능한 캠핑장을 검색합니다. (크기 필터링 가능) |

---

## 2. 핵심 비즈니스 규칙

### 2.1. 예약 규칙

- **예약 가능 기간**:
  - 예약 시작일은 과거일 수 없습니다.
  - 종료일은 시작일보다 이전일 수 없습니다.
  - 최대 예약 기간은 30일로 제한됩니다.
- **고객 정보**:
  - 예약자 이름은 필수이며, 2자 이상 20자 이하여야 합니다.
  - 전화번호는 `01`로 시작하는 10자리 또는 11자리 숫자여야 합니다.
- **중복 예약**:
  - 동일한 캠핑장에 대해 기간이 겹치는 중복 예약은 불가능합니다.
- **예약 식별**:
  - 예약 생성 시 6자리의 영문/숫자 조합으로 된 `확인 코드`가 발급됩니다.
  - 예약을 수정하거나 취소할 때 이 확인 코드가 반드시 필요합니다.
- **예약 취소**:
  - 예약 시작일 당일에 취소하면 `CANCELLED_SAME_DAY` 상태가 됩니다.
  - 그 외의 경우 `CANCELLED` 상태가 됩니다.

### 2.2. 가격 정책

- **기본 가격** (1박 기준):
  - 대형 사이트 (A로 시작): 80,000원
  - 소형 사이트 (B로 시작): 50,000원
  - 기타: 60,000원
- **할증 규칙**:
  - **주말 (토, 일)**: 30% 할증
  - **성수기 (7월, 8월)**: 50% 할증
  - **성수기 주말**: 70% 할증

### 2.3. 포인트 적립 규칙

- **기본 적립**: 결제 금액의 5%
- **주말 포함 시**: 결제 금액의 10%
- **성수기 (주말 제외)**: 결제 금액의 3% (기본 적립률보다 낮아 확인 필요)

---

## 3. 잠재적 버그 및 코드 스멜

### 3.1. 심각한 동시성 문제 (Concurrency Issue)

- **위치**: `ReservationService.createReservation()`
- **문제**: 예약 가능 여부를 확인(`hasConflict`)한 후, 실제 데이터베이스에 저장(`save`)하기 전에 `Thread.sleep(100)`이 존재합니다. 이는 **TOCTTOU (Time-of-check to time-of-use)** 라는 심각한 Race Condition(경쟁 상태) 버그를 유발합니다.
- **위험**: 두 명의 사용자가 거의 동시에 동일한 날짜에 예약을 시도할 경우, 두 요청 모두 예약이 가능하다고 판단하여 **중복 예약이 발생**할 수 있습니다. `@Transactional`이 적용되어 있지만, 기본 격리 수준(Isolation Level)에서는 이 문제를 막지 못할 수 있습니다.
- **개선 제안**: 데이터베이스의 비관적 락(Pessimistic Lock, `SELECT ... FOR UPDATE`) 또는 낙관적 락(Optimistic Lock, `@Version`)을 사용하여 동시성을 제어해야 합니다.

### 3.2. 거대하고 복잡한 메서드 (God Method & Deep Nesting)

- **위치**: `ReservationService.createReservation()`
- **문제**: 하나의 메서드가 100줄이 넘으며, **입력값 검증, 가격 계산, 포인트 계산, 예약 가능 여부 확인, 객체 생성, DB 저장, 알림 전송** 등 너무 많은 책임을 가지고 있습니다. 또한, `if-else`가 여러 단계로 중첩되어 코드의 복잡도가 매우 높습니다.
- **위험**: 코드를 이해하고 수정하기 어려워 유지보수 비용이 증가하며, 새로운 버그가 발생할 가능성이 높습니다.
- **개선 제안**:
  - **책임 분리**: 가격 계산, 포인트 계산, 검증 로직 등을 별도의 클래스(예: `PricingService`)나 private 메서드로 분리해야 합니다.
  - **가드 클로저 (Guard Clauses)**: `if-else` 중첩 대신, 조건이 맞지 않으면 빠르게 `return`하거나 `Exception`을 던지는 방식으로 코드를 평탄하게 만들어야 합니다.

### 3.3. 비효율적인 데이터 조회

- **위치**: `ReservationService.getMonthlyCalendar()`, `getDailyReservationCount()` 등 다수
- **문제**: `reservationRepository.findAll()`을 호출하여 모든 예약 데이터를 메모리에 로드한 후, 자바 스트림을 이용해 필터링합니다.
- **위험**: 데이터베이스의 규모가 커질수록 심각한 성능 저하와 메모리 부족(OOM)을 유발할 수 있습니다.
- **개선 제안**: JPQL이나 QueryDSL을 사용하여 처음부터 필요한 데이터만 조회하도록 쿼리를 최적화해야 합니다. (예: `WHERE` 절에 날짜, 사이트 ID 등의 조건을 추가)

### 3.4. 코드 중복 (Duplicated Code)

- **문제**: 동일하거나 유사한 코드가 여러 곳에 반복해서 나타납니다.
  - **가격 계산 로직**: `createReservation`, `processReservationWithPayment`, `calculateReservationPrice` 등 여러 곳에 중복됩니다.
  - **유효성 검증 로직**: `ValidationUtils`가 있음에도 불구하고, 각 서비스 메서드(`createReservation`, `updateReservation` 등) 내에서 직접 검증 로직을 구현하고 있습니다.
  - **DTO 변환 로직**: `ReservationResponse.from()`이라는 변환 메서드가 있지만, 많은 곳에서 수동으로 객체를 변환하고 있습니다.
- **위험**: 로직 변경 시 모든 중복 코드를 찾아 수정해야 하므로 실수가 발생하기 쉽고 유지보수가 어렵습니다.
- **개선 제안**: 중복 코드를 하나의 메서드나 클래스로 통합하고, 이를 재사용하도록 리팩토링해야 합니다.

### 3.5. 하드코딩된 값 (Hardcoded Values)

- **문제**: 예약 상태(`"CANCELLED"`), 사이트 종류를 구분하는 문자열(`"A"`, `"B"`), 가격, 포인트 적립률과 같은 "매직 넘버"들이 코드 전반에 하드코딩되어 있습니다.
- **위험**: 정책 변경 시 관련된 모든 코드를 찾아 수정해야 하며, 오타로 인한 버그가 발생하기 쉽습니다.
- **개선 제ANALYZE_REPORT.md**: `Enum` 또는 `static final` 상수를 사용하여 이러한 값들을 중앙에서 관리해야 합니다.

### 3.6. 일반적인 예외 처리 (Generic Exception Handling)

- **위치**: `ReservationController`, `SiteController`
- **문제**: `catch (RuntimeException e)`와 같이 최상위 예외를 잡아서 모두 동일한 HTTP 상태 코드(예: 400, 500)와 단순한 오류 메시지를 반환합니다.
- **위험**: 클라이언트(프론트엔드)가 오류의 원인을 파악하기 어려워 적절한 사용자 피드백을 제공할 수 없습니다.
- **개선 제안**: `ReservationNotFoundException`과 같이 의미 있는 커스텀 예외를 정의하고, `@ControllerAdvice`를 사용하여 각 예외에 맞는 HTTP 상태 코드와 상세한 오류 정보를 반환하도록 구현해야 합니다.
