# 리스크 매트릭스 (재작성)

## 1. 평가 기준
- **척도**: 높음(3점), 보통(2점), 낮음(1점)
- **평가 항목**:
    - **비즈니스 임팩트**: 기능 실패 시 비즈니스에 미치는 영향의 크기
    - **코드 복잡도**: 기능 구현 코드의 이해, 수정, 테스트의 어려움 (Nesting, 결합도 등)
    - **변경 빈도**: 향후 비즈니스 요구사항 변경으로 코드가 수정될 가능성
    - **버그 가능성**: 코드 스멜, 잘못된 설계로 인해 버그가 존재하거나 발생할 잠재력
- **계산식**: 리스크 점수 = 비즈니스 임팩트 + 코드 복잡도 + 변경 빈도 + 버그 가능성

## 2. 평가 결과

| 기능 | 비즈니스 임팩트 | 코드 복잡도 | 변경 빈도 | 버그 가능성 | 총점 | 순위 |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|
| **예약 생성 & 동시성 제어** | 높음(3) | 높음(3) | 높음(3) | 높음(3) | **12** | **1** |
| **가용성 확인 (캘린더 포함)** | 높음(3) | 보통(2) | 보통(2) | 높음(3) | **10** | **2** |
| **예약 수정 및 취소** | 보통(2) | 보통(2) | 낮음(1) | 보통(2) | **7** | **3** |
| **예약 조회** | 낮음(1) | 낮음(1) | 낮음(1) | 보통(2) | **5** | **4** |
| **사이트 조회** | 낮음(1) | 낮음(1) | 낮음(1) | 낮음(1) | **4** | **5** |

## 3. 테스트 우선순위 추천

`SYSTEM_ANALYSIS_REPORT.md`의 분석 결과를 바탕으로, 아래 순서에 따라 테스트를 집중하는 것을 추천합니다.

### Priority 1: 예약 생성 및 동시성 제어 (가장 시급)

- **선정 이유**:
    - **치명적 결함 존재**: 분석 리포트에서 지적된 **"심각한 동시성 문제(Race Condition)"**는 중복 예약을 유발하여 직접적인 매출 손실과 데이터 정합성 파괴로 이어집니다.
    - **극도로 높은 복잡도**: **"거대하고 복잡한 메서드(God Method)"**인 `createReservation`은 수많은 책임과 중첩된 로직으로 인해 작은 수정에도 예측 불가능한 버그를 유발할 가능성이 매우 높습니다. 리스크 총점(12점)이 압도적으로 높아 최우선 순위로 해결해야 합니다.
- **필수 테스트 항목**:
    - **동시성/스트레스 테스트**: 여러 스레드(사용자)가 동시에 동일한 사이트/날짜에 예약을 요청하는 시나리오를 자동화하여, **중복 예약이 절대 발생하지 않음**을 반드시 검증해야 합니다. (예: `CountDownLatch`, 테스트 컨테이너 활용)
    - **시나리오 기반 통합 테스트**: 리팩토링 후, API 호출부터 데이터베이스 저장까지 전체 흐름을 검증합니다. 모든 유효성 검사(날짜, 이름, 기간)의 성공/실패 케이스를 포함해야 합니다.
    - **단위 테스트**: 가격 계산, 포인트 계산, 유효성 검증 로직이 별도의 클래스로 분리된 후, 각 모듈의 동작을 독립적으로 검증합니다.

### Priority 2: 가용성 확인 (캘린더 포함)

- **선정 이유**:
    - **심각한 성능 저하 위험**: `findAll()` 후 메모리에서 필터링하는 **"비효율적인 데이터 조회"** 방식은 데이터 증가 시 반드시 성능 병목과 서버 다운을 유발합니다. 이는 고객이 예약을 시작하는 첫 관문을 막는 심각한 문제입니다.
- **필수 테스트 항목**:
    - **성능 테스트 (Before/After)**: `findAll()`을 사용하는 API에 대해 대용량 데이터를 삽입한 후, 응답 시간을 측정합니다. JPQL/QueryDSL 등으로 쿼리를 수정한 뒤, 동일한 조건에서 성능이 개선되었는지 수치적으로 비교 검증해야 합니다.
    - **정확성 통합 테스트**: 예약 생성/취소 직후, 해당 날짜가 캘린더와 예약 가능 여부 조회 API 결과에 즉시, 그리고 정확하게 반영되는지 검증합니다. (특히 연박 예약의 모든 중간 날짜 포함)

### Priority 3: 예약 수정/취소 및 조회

- **선정 이유**:
    - **코드 일관성 및 유지보수성 저하**: 분석 리포트에서 지적된 **"코드 중복"**과 **"일반적인 예외 처리"**는 기능의 신뢰성을 떨어뜨리고 유지보수를 어렵게 만듭니다.
- **필수 테스트 항목**:
    - **API Contract 테스트**: 잘못된 확인 코드를 입력했을 때, `400 Bad Request`와 명확한 에러 메시지가 반환되는지, 존재하지 않는 예약을 조회했을 때 `404 Not Found`가 반환되는지 등, 각 예외 상황에 맞는 HTTP 응답이 오는지 검증합니다.
    - **기능별 통합 테스트**: 각 기능(수정, 취소, 이름/전화번호로 조회)이 명세에 따라 정확히 동작하는지 확인합니다. 수정 시 일부 필드만 변경되는 경우도 테스트해야 합니다.