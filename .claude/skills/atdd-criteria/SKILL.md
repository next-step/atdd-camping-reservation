---
name: atdd-criteria
description: ATDD 1단계(인수 조건). 티켓을 받아 코드의 지금 동작을 파악하고 요구사항과 어긋나는 자리를 판정해 acceptance-criteria.md에 남긴다. 다음 단계(atdd-test)는 사용자가 직접 호출해야 진행된다.
---

# 1. 인수 조건

`docs/principles.md`, `docs/plan.md`를 먼저 읽는다.

## 입력

- 인자로 받은 티켓 번호(예: T-3). `docs/tickets.md`에서 해당 티켓 내용을 찾는다.
- 이 티켓에서 다룰 요구사항. 호출 시 인자(예: `/atdd-criteria T-3 <추가 요구사항>`)로
  같이 받을 수 있다. 티켓 번호 뒤에 추가 텍스트가 있으면 그것을 이번 판정에서 반영할
  요구사항으로 취급한다. 없으면 `docs/tickets.md`에 적힌 내용만으로 판정한다.

## 하는 일

1. 관련 코드를 읽어 지금 동작을 실측으로 파악한다. 필요하면 서버를 띄워
   `/api` 엔드포인트를 직접 호출해 응답을 확인한다(추측 금지 — `principles.md`의
   "실측에 근거한 판정").
2. 요구사항과 코드의 지금 동작이 어긋나는 자리를 찾아 판정한다.
3. 요구사항이 침묵하는 자리(정할 수 없는 것)를 만나면 답을 만들지 않는다.

## 남기는 것

- `docs/acceptance-criteria.md`에 이 티켓의 규칙, 이유, Given·When·Then 예시(직접 호출해
  받은 요청/응답 JSON 기반), 못 정한 것은 그 아래 "질문"으로 적는다.
- 질문을 남겼으면 반드시 `docs/tickets.md`에 짝이 되는 티켓도 같이 추가한다. 질문만
  남기고 티켓을 빠뜨리지 않는다.
- 남은 일(추가로 발견한 결함, 별도 티켓감)은 `docs/tickets.md`에 추가한다.
- `docs/tickets.md`에 이미 같은 내용이 티켓으로 있으면 `acceptance-criteria.md`에 산문으로
  중복 설명을 남기지 않는다.

## AI에게 주는 지시

- 규칙은 "무엇이 참이어야 하는가"로 쓴다. 특정 메서드·엔드포인트·구현 동작
  ("~하면 거부한다", "createReservation은 ~검사한다")에 종속되게 쓰지 않는다. 도메인 참으로
  쓰면 같은 규칙이 지켜져야 할 다른 경로(수정 등)가 문장만 보고도 드러난다.
- 규칙/이유/Given·When·Then/질문 블록은 코드블록(```)으로 감싼다. 마크다운은 빈 줄 없는
  연속된 줄을 한 문단으로 합쳐버리고, 트레일링 스페이스로 줄바꿈을 강제하는 방식은
  린터가 지울 수 있다.
- 예시는 직접 호출해 받은 실제 응답으로 채운다. 한 줄로 압축하지 않고 코드블록으로 펼쳐
  사람이 읽기 좋게 정리한다. 날짜처럼 "오늘" 기준 상대값이 들어간 예시는 재검증 시점의
  오늘 날짜로 다시 호출해 갱신한다. 오래된 "오늘" 기준 예시를 그대로 남기지 않는다.
- "현재 상태"는 코드를 다시 읽고 실측해서 판정한다. 이전에 미구현(버그)이었더라도 그
  사이 커밋된 수정이 있으면 다시 확인해 갱신한다 — 문서가 코드보다 뒤처진(stale) 채로
  남지 않게 한다.
- 요구사항이 침묵하는 자리를 스스로 판단해 채우지 않는다. AskUserQuestion으로 사용자에게
  묻거나, 답이 지금 필요하지 않으면 질문으로 남기고 넘어간다. 질문 자리의 버그 여부·
  사양 여부는 AI가 판정하지 않는다.
- 새로 발견한 회색지대를 문서에 넣을지는 먼저 사람에게 확인받고 진행한다. 확인 없이
  임의로 추가하지 않는다.
- "현재 상태"는 간결하게 쓴다. 파일 경로와 핵심 실측 결과만 남기고, 커밋 해시·전체
  요청/응답 JSON·"구현되어 있고 정상 동작함" 같은 군더더기 표현은 뺀다.
- 규칙/이유는 tickets.md → acceptance-criteria.md 방향으로 만들어진다.
  acceptance-criteria.md에서 tickets.md 항목을 "참고"하라고 역참조하지 않는다(원천과
  결과물의 방향이 바뀐다).
- 이미 다른 티켓이나 acceptance-criteria.md 항목이 같은 내용을 다루면, 그 내용을 옮겨
  적어 합치지 말고 중복 티켓 자체를 제거한다.
- 코드를 고치지 않는다. 이 단계는 판정만 한다.

## 넘어가기 전에 (하드 게이트)

`acceptance-criteria.md`에 남긴 예시가 실제로 직접 호출해 받은 응답인지 사용자에게
보여주고 확인받는다.

**이 스킬은 여기서 끝난다.** 2단계(인수 테스트)는 자동으로 이어서 하지 않는다.
사용자가 `/atdd-test <티켓번호>`를 직접 호출할 때만 다음 단계가 시작된다는 것을
사용자에게 명시적으로 알리고 턴을 종료한다.
