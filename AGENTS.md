# 프로젝트 요약 및 작업 방식
- 서비스: Spring Boot 기반 초록 캠핑장 예약 웹 애플리케이션으로, 캠핑 사이트 조회·기간별 예약 가능 여부 검색·예약 생성/조회/수정/취소와 캘린더를 제공한다.
- 핵심 도메인 객체는 `Campsite`(사이트 번호·설명·최대 인원·예약 목록)와 `Reservation`(고객·예약 기간·사이트·상태·확인 코드)이며, 둘은 1:N/다대일로 연결된다.
- 작업은 `docs/plan.md` 순서에 따라 인수 조건을 먼저 정하고, 인수 테스트를 작성·실패 확인한 뒤 최소 구현하고 전체 테스트로 판정하는 ATDD 방식이다.
- 판단은 코드 설명보다 실제 API 호출과 응답을 근거로 하며, 데이터 관련 작업 전 `src/main/resources/data.sql`을 확인하고 문서 결과는 `docs/`에 쌓는다.
- 실행은 Java 17+와 `./gradlew bootRun`(기본 8080), 테스트는 `./gradlew test`를 사용하며 캐시가 의심되면 `./gradlew test --rerun-tasks`로 재실행한다.
