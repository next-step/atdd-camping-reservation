# 테스트 실행 결과

> 실행일: 2026-01-17
> 총 테스트: 38개 | 성공: 26개 | 실패: 7개

---

## 실패한 테스트 목록

### 예약 생성 (ReservationCreateAcceptanceTest)

- [x] **연락처 없이 예약할 수 없다**
  - 예상: `409 CONFLICT`
  - 실제: `201 CREATED`
  - 원인: 프로덕션 코드에서 연락처(phoneNumber) 필수 검증 미구현
  - 파일: `ReservationService.java:117-132`

- [ ] **두 고객이 동시에 같은 사이트를 예약하면 한 명만 성공한다**
  - 예상: 성공 1건, 실패 1건
  - 실제: 성공 2건
  - 원인: 동시성 제어(락) 미구현 - 두 요청 모두 예약 성공
  - 파일: `ReservationService.java` (트랜잭션 격리 수준 또는 비관적 락 필요)

- [x] **취소된 예약이 있는 날짜는 다시 예약할 수 있다**
  - 예상: `201 CREATED`
  - 실제: `409 CONFLICT`
  - 원인: 예약 중복 검사 시 취소된 예약(CANCELLED) 상태 필터링 미구현
  - 파일: `ReservationRepository.java` - `existsByCampsiteAndStartDateLessThanEqualAndEndDateGreaterThanEqual` 쿼리에 상태 조건 추가 필요

### 예약 수정 (ReservationUpdateAcceptanceTest)

- [ ] **다른 예약이 있는 날짜로 변경할 수 없다**
  - 예상: `400 BAD_REQUEST`
  - 실제: `200 OK`
  - 원인: 날짜 변경 시 기존 예약과의 중복 검사 미구현
  - 파일: `ReservationService.java:358-431` - updateReservation 메서드

- [ ] **해당 기간에 예약된 사이트로는 변경할 수 없다**
  - 예상: `400 BAD_REQUEST`
  - 실제: `200 OK`
  - 원인: 사이트 변경 시 해당 기간 예약 중복 검사 미구현
  - 파일: `ReservationService.java:397-401` - 사이트 변경 로직

### 사이트 검색 (SiteSearchAcceptanceTest)

- [ ] **비어있는 사이트만 검색 결과에 표시된다**
  - 예상: `["A-2", "B-1"]` (A-1은 예약됨)
  - 실제: `["A-1", "A-2", "B-1"]`
  - 원인: 검색 시 예약된 사이트 필터링 미구현
  - 파일: `SiteService.java` - 검색 쿼리에 예약 상태 조건 필요

- [ ] **모든 사이트가 예약된 경우 검색 결과가 없다**
  - 예상: 빈 배열 `[]`
  - 실제: `["A-1", "A-2", "B-1"]`
  - 원인: 검색 시 예약된 사이트 필터링 미구현 (위와 동일 원인)
  - 파일: `SiteService.java`

---

## 수정 우선순위

### 높음 (비즈니스 핵심 기능)
1. 취소된 예약 필터링 - 예약 중복 검사 쿼리 수정
2. 사이트 검색 시 예약 필터링 - 검색 로직 수정
3. 예약 수정 시 중복 검사 - updateReservation 로직 보완

### 중간 (데이터 정합성)
4. 동시성 제어 - 비관적 락 또는 낙관적 락 적용
5. 연락처 필수 검증 - 유효성 검사 로직 추가


---

## 요약

| 카테고리 | 실패 수 | 원인 |
|---------|------|------|
| 예약 생성 | 3    | 검증 로직 미구현, 동시성 미처리 |
| 예약 수정 | 2    | 중복 검사 미구현 |
| 사이트 검색 | 2    | 예약 필터링 미구현, 테스트 오류 |